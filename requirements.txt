# For GPU support, it is highly recommended to install PyTorch manually first
# using the --index-url that matches your system's CUDA toolkit.
# Example for CUDA 12.1:
# pip install torch==2.5.1 torchvision==2.5.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu121

# Core dependencies
torch
torchvision
torchaudio
faster-whisper==1.2.1
ffmpeg-python==0.2.0
numpy==2.3.2
pyperclip

# Hardware Monitoring
psutil
pynvml

# Optional dependency for GUI theme
ttkthemes==3.3.0
tkinterdnd2
gradio


# Additional pinned packages added after environment sync
# (these are required by the app at runtime or by faster-whisper/hf tooling)
huggingface-hub==0.36.0
ctranslate2==4.6.1
typing_extensions==4.15.0
